% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/task-start.R
\name{start_task_process}
\alias{start_task_process}
\title{Launch a background task in a child R session.}
\usage{
start_task_process(
  id,
  expr,
  result_path = NULL,
  import = NULL,
  packages = NULL,
  workdir = NULL,
  env = NULL,
  save_result = TRUE
)
}
\description{
Purpose:
\itemize{
\item Start one asynchronous child process and return a \code{Task} object that wraps
the session handle.
}
}
\details{
Parameters:
\itemize{
\item \code{id}: Task identifier used by the package registry.
\item \code{expr}: User expression to run in the child process.
\item \code{result_path}: Optional path where the child should save its result.
When \code{NULL}, the standard \code{task_tmpfile(id)} path is used.
\item \code{import}: Optional character vector of object names to copy from the main
session \code{.GlobalEnv} into the child process.
\item \code{packages}: Optional character vector of package names to load in the
child process.
\item \code{workdir}: Optional working directory for the child process.
\item \code{env}: Optional named list of environment variables to set in the child
process.
\item \code{save_result}: Whether task output should be written to result storage.
}

Returns:
\itemize{
\item A \code{Task} object in \code{running} state.
}

Assumptions and side effects:
\itemize{
\item Requires the \code{callr} package to be installed.
\item Starts a new background R session immediately.
\item The child process runs asynchronously after this function returns.
}
}
\keyword{internal}
