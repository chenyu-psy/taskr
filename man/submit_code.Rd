% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/submit.R
\name{submit_code}
\alias{submit_code}
\title{Submit an Expression Task to the Queue}
\usage{
submit_code(
  expr,
  label = NULL,
  priority = 0L,
  resources = list(slots = 1L),
  import = "auto",
  output = "none"
)
}
\arguments{
\item{expr}{Expression to evaluate in a child process.}

\item{label}{Optional user-facing task label.}

\item{priority}{Integer-like priority. Larger values run earlier.}

\item{resources}{Resource declaration list. v0.4 uses \code{list(slots = 1)}.}

\item{import}{Task context import mode. Use \code{"auto"} (default), \code{"none"},
or \code{list(env = ..., packages = ..., workdir = ..., vars = ...)}.}

\item{output}{Either \code{"none"} or \code{"all"}. \code{"none"} skips result file
saving and is the default. Use \code{"all"} when you need
\code{get_task_result()} to return the expression value.}
}
\value{
Invisibly returns \code{NULL}.
}
\description{
Purpose:
\itemize{
\item Capture one expression and enqueue it for background execution.
}
}
\examples{
init_queue(max_slots = 1)
if (interactive() && requireNamespace("callr", quietly = TRUE)) {
  on.exit(shutdown_queue(), add = TRUE)
  submit_code({ 1 + 1 }, label = "toy_expr", output = "all")
}
}
