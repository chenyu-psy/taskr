% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/submit.R
\name{map_tasks}
\alias{map_tasks}
\title{Submit a Grid of Function Calls to the Queue}
\usage{
map_tasks(
  fun,
  grid,
  fixed_args = list(),
  ...,
  label_fmt = NULL,
  priority = 0L,
  resources = list(slots = 1L),
  import = "auto",
  output = "none"
)
}
\arguments{
\item{fun}{Function object to run for each grid row.}

\item{grid}{Data frame or list of named lists. Each row/item defines one run.}

\item{fixed_args}{Named list of shared arguments merged into every call.}

\item{...}{Additional named arguments. Names prefixed with \code{fun_} are treated
as dynamic generators and must be functions.}

\item{label_fmt}{Optional label template with \code{{column}} placeholders.}

\item{priority}{Integer-like priority for all generated tasks.}

\item{resources}{Resource declaration list. v0.4 uses \code{list(slots = 1)}.}

\item{import}{Task context import mode. Use \code{"auto"} (default), \code{"none"},
or \code{list(env = ..., packages = ..., workdir = ..., vars = ...)}.}

\item{output}{Output policy passed to \code{submit_task()}. Defaults to \code{"none"}
to avoid writing large batch outputs unless requested.}
}
\value{
Invisibly returns \code{NULL}.
}
\description{
Purpose:
\itemize{
\item Expand a parameter grid into multiple \code{submit_task()} submissions.
\item Support \code{fun_} dynamic argument generators based on each grid row.
}
}
\examples{
init_queue(max_slots = 1)
grid <- data.frame(model = c("a", "b"), stringsAsFactors = FALSE)
if (interactive() && requireNamespace("callr", quietly = TRUE)) {
  on.exit(shutdown_queue(), add = TRUE)
  map_tasks(
    fun = function(formula) formula,
    grid = grid,
    fun_formula = function(model) paste0("y ~ ", model),
    label_fmt = "fit_{model}"
  )
}
}
