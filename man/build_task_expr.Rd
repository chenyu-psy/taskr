% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/task-start.R
\name{build_task_expr}
\alias{build_task_expr}
\title{Internal Helpers to Launch Background Tasks}
\usage{
build_task_expr(
  expr,
  result_path = NULL,
  status_path = NULL,
  imports = list(),
  packages = NULL,
  workdir = NULL,
  env = NULL
)
}
\description{
This file contains the first subprocess launcher used by \code{taskr}. The helper
builds a wrapped expression that evaluates user code in a child R session and
optionally saves the result to disk. The launcher returns an internal \code{Task}
object so later queue code can monitor the running process.
Build a wrapped expression for a background task.
}
\details{
Purpose:
\itemize{
\item Create the exact expression that the child R session should evaluate.
}

Parameters:
\itemize{
\item \code{expr}: User expression to run in the child process.
\item \code{result_path}: Optional file path where the child should save its result.
\item \code{status_path}: Optional file path where the child should write task
lifecycle status markers.
\item \code{imports}: Named list of objects to assign into the child global
environment before evaluating \code{expr}.
\item \code{packages}: Optional character vector of package names to load in the
child process before evaluating \code{expr}.
\item \code{workdir}: Optional working directory for the child process.
\item \code{env}: Optional named list of environment variables for the child process.
}

Returns:
\itemize{
\item An R language object suitable for submission to \code{callr::r_session}.
}

Assumptions and side effects:
\itemize{
\item The expression is evaluated in the child process, not in the caller.
\item When \code{result_path} is provided, the child writes one \code{.rds} file.
\item When \code{status_path} is provided, the child writes one status marker file.
\item Package loading, working directory changes, and environment variable
updates happen in the child process only.
}
}
\keyword{internal}
